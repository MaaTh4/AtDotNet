@model IEnumerable<PersonagemCreator>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.classe)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.poder)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.raca)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.classe)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.poder)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.raca)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.age">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.age">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.age">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
